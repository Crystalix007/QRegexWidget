CMake Qt5 GUI Template
----------------------

You need Qt5 and CMake 2.8.11 or higher

-- Build instructions --

cd ~/some/dir/
git clone https://github.com/Crystalix007/QRegexWidget.git
cd QRegexWidget

For Arch Linux users (or other users using pacman):

makepkg -ef --install

Else:

cd build
cmake ../
ninja

./qregexwidget

And to install:

ninja install


-- Tutorials and resources --

Qt online docs
https://doc.qt.io/
